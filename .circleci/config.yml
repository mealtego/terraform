jobs:
  
  validate:
    machine: true 
    steps:
      - checkout
      
      - run:
          command: |
            docker pull accurics/terrascan
            docker run --rm -w /src -v $(pwd):/src accurics/terrascan scan -d gcp -o json | tee terrascan-output.json || true
          
      - store_artifacts:
          path: terrascan-output.json
          destination: terrascan-output
          when: always 


  build:
    machine: true
    steps:
      - checkout
      - run: echo "This is a build step"

  test:
    machine: true
    steps:
      - checkout
      - run: echo "This is a test step"

  integration:
    machine: true
    steps:
      - checkout
      - run:
          command: |
            echo "This is an integration step"
          when: on_fail

  prod:
    machine: true
    steps:
      - checkout
      - run: echo "This is a deploy step"

workflows:
  version: 2
  terraform:
    jobs:
      - validate
      - build:
          requires:
            - validate
      - test:
          requires:
            - build 
      - integration:
          requires:
            - test
      - prod:
          type: approval
          requires:
            - integration